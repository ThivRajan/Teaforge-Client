// import { Games } from './types';
export const SERVER_URI = 'http://localhost:3001/';

// export const playerCount = Object();
// playerCount[Games.Resistance] = [5, 10];

export const playerCounts = { 'resistance': 5 };

